import { EntreprisePipe } from './entreprise.pipe';

describe('EntreprisePipe', () => {
  it('create an instance', () => {
    const pipe = new EntreprisePipe();
    expect(pipe).toBeTruthy();
  });
});
